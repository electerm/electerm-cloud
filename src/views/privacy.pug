// Privacy page template
//- Expected variables: privacyData, headerLinks, headerUrls, footerSections, siteMeta, lang, t, cssUrl, canonicalPath
doctype html
html(lang=lang === 'cn' ? 'zh-CN' : 'en')
  head
    include partials/head
  body
    include partials/header

    main.static-main
      .static-inner
        h1.static-title= t(privacyData.content.title)
        p.static-updated= t(privacyData.content.lastUpdated)
        each section in privacyData.content.sections
          article.static-section
            h2.static-section-title= t(section.title)
            .static-section-body
              p= t(section.body)

    include partials/footer
    include partials/analytics
    include partials/ethical-ads
